- defaults:
    link:
        relink: true

- clean: ['~']

- link:
    ~/.bash_profile:
    ~/.bashrc:
    ~/.shell:
    ~/.pryrc:

- shell:
    - [git submodule update --init --recursive, Installing submodules]
